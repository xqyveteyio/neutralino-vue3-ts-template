{
  "name": "neutralino-vue3-ts-template",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc -b && vite build",
    "preview": "vite preview",
    "setup": "neu update && npm run build",
    "app:dev": "node scripts/dev.mjs",
    "app:build": "npm run build && neu build --release"
  },
  "dependencies": {
    "@neutralinojs/lib": "^6.8.0",
    "vue": "^3.5.0",
    "vue-router": "^5.0.0"
  },
  "devDependencies": {
    "@neutralinojs/neu": "^11.7.2",
    "@vitejs/plugin-vue": "^6.0.0",
    "typescript": "~5.9.0",
    "vite": "^7.0.0",
    "vue-tsc": "^3.0.0"
  }
}
